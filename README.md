# WebRendererMCP

## Description

WebRendererMCP renders HTML using Playwright and exposes it as an MCP server.

## Installation

```
{
  "mcpServers": {
    "WebRenderer": {
      "command": "npx",
      "args": ["web-renderer-mcp"]
    }
  }
}
```
